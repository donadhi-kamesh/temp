# Workspace Rules - Tuition Fee Payment Portal

- **No Local Servers or Tunnels:** Do not start or run any local HTTP servers, local tunnels (like localhost.run), or reverse proxies. Pushing code updates directly to the GitHub repository (`donadhi-kamesh/temp`) is sufficient for deployment and preview.
